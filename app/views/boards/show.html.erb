<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @board.name %>
</p>

<p>
  <strong>About:</strong>
  <%= @board.about %>
</p>

<p>
  <strong>User:</strong>
  <%= @board.user.full_name %>
</p>

<p>
  <strong>Associated Pins:</strong>
  <% @pins.each do |pin|%>
    <%= image_tag(pin.image_url, :width => 500) if pin.image.present? %>
  <% end %>
</p>

<%= link_to 'Edit', edit_board_path(@board) %> |
<%= link_to 'Back', boards_path %>
